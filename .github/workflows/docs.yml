name: Docs

on:
  push:
    branches: [ main ]
    paths:
      - 'mkdocs.yml'
      - 'docs/**'
      - 'dsg-jit/src/**'
      - '.github/workflows/docs.yml'
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write   # needed for mkdocs gh-deploy to push to gh-pages

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          # Core deps from your repo (if you want)
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
          # Docs deps
          pip install "mkdocs>=1.5" "mkdocs-material" "mkdocstrings[python]"

          # Optional: install your package so mkdocstrings can import it
          if [ -d "dsg-jit" ]; then pip install -e ./dsg-jit; fi

      - name: Build docs
        run: mkdocs build --strict

      - name: Deploy docs to GitHub Pages
        if: github.ref == 'refs/heads/main'
        run: mkdocs gh-deploy --force